export enum HttpCode {
  Success = 'OK',
  InternalServerError = 500,
  Unauthorized = 'token_err',
}

export enum AppTheme {
  Light = 'light',
  Dark = 'dark',
}

export enum RoleName {
  Admin = 'admin',
  User = 'user',
}
